一.科普管理
1.罕见病列表
	A.查询条件不起作用。
	B.列表：
		B1.上线时间值未显示出来。
		B2.点击 编辑 后，有时 辅助检查 内容显示会变形。
		B3.每行记录展开后的字段，尽量展示剩余全部字段（是文件的字段显示文件名，并能够支持下载）。
	C.新增/编辑：
		C1.诊断流程文件 与 附件 是否需要提供文件上传按钮，并且编辑时能够删除不需要的文件。
		C2.上线时间 未保存。
		C3.备注 未保存。

2.罕见病关联
	A.新增后未保存，列表中没有显示新增的记录。

二.项目管理
1.项目列表
	A.查询条件不起作用。
	B.列表：
		B1.创建时间 与 更新时间 没有值显示。
		B2.操作一列中 增加 项目二维码 操作，点击后出现一个可以扫描的二维码，扫描后显示项目的详细信息。
		B3.项目负责人 没有值显示。
		B4.删除 点击后 界面报错。
	C.新增/编辑：
		C1.增加 医院 字段选择（非必填）。
		C2.项目背景 输入框修改为 大文本框，独立一行。
		C3.项目状态 修改为 字典选项 目前包含：进行中、中断、筛选中、终止、结束⋯⋯。
		C4.去除 项目二维码 字段。

2.加入者列表
	A.查询条件不起作用。
	B.列表：
		B1.本模块字段少，能够第一层展示的都在第一层中进行展示。
		B2.删除 操作时报错，无法删除。
		B3.有未翻译为中文的列头，需要翻译为中文，如：logId。
		B4.创建时间 与 更新时间 没有值显示。
	C.新增/编辑：
		C1.编辑 保存时报错，无法保存。
		C2.项目 与 状态 更为为字典选项，项目选项为其同级菜单 项目列表 中的项目；状态 可先设置同意加入、拒绝加入、退出⋯⋯
		C3.名称 修改为 加入者姓名。
		C4.增加 加入者联系电话、加入者联系地址字段。

三.企业人事管理
1.企业管理
	A.查询条件不需要精确匹配，需要全字段模糊匹配。
	B.列表：
		B1.本模块字段少，能够第一层展示的都在第一层中进行展示。
		B2.删除 操作时需要增加其它模块是否已有关联，关联时给出提醒。
	C.新增/编辑：
		C1.需增加 状态 字段，用来区分该企业是否可用。
		C2.名称 需要增加重复校验。

2.医生管理
	A.查询条件不起作用。
	B.列表：
		B1.本模块字段少，能够第一层展示的都在第一层中进行展示。
		B2.删除 操作时需要增加其它模块是否已有关联，关联时给出提醒。
	C.新增/编辑：
		C1.需增加 状态 字段，用来区分该医生是否可以被关联。
		C2.编辑时增加用户重复校验。

3.医院管理
	A.查询条件不需要精确匹配，需要全字段模糊匹配。
	B.列表：
		B1.本模块字段少，能够第一层展示的都在第一层中进行展示。
		B2.删除 操作时需要增加其它模块是否已有关联，关联时给出提醒。
	C.新增/编辑：
		C1.需增加 状态 字段，用来区分该医院是否可用。
		C2.名称 需要增加重复校验。

4.志愿者管理
	A.查询条件不起作用。
	B.列表：
		B1.本模块字段少，能够第一层展示的都在第一层中进行展示。
		B2.删除 操作时需要增加其它模块是否已有关联，关联时给出提醒。
	C.新增/编辑：
		C1.需增加 状态 字段，用来区分该志愿者是否可用可选。
		C2.编辑时 名称 需要增加重复校验。
		C3.编辑 保存时 报logId不存在错误。
		C4.新增保存 详细地址 字段没有保存。

5.机构管理
	A.查询条件不需要精确匹配，需要全字段模糊匹配。
	B.列表：
		B1.本模块字段少，能够第一层展示的都在第一层中进行展示。
		B2.删除 操作时需要增加其它模块是否已有关联，关联时给出提醒。
	C.新增/编辑：
		C1.需增加 状态 字段，用来区分该机构是否可用可选。
		C2.新增/编辑 时 名称 需要增加重复校验。
		C3.新增/编辑 保存 备注 字段没有保存。

四.活动管理
1.奖项配置
	A.查询条件不起作用。
	B.列表：
		B1.本模块字段少，能够第一层展示的都在第一层中进行展示。
		B2.删除 操作时需要增加其它模块是否已有关联，关联时给出提醒。
		B3.类型 等级，字段字典没有翻译。
	C.新增/编辑：
		C1.需增加 状态 字段，用来区分该活动是否可用可选。
		C2.新增/编辑 时 需增加活动别名 需要增加重复校验。

2.奖励记录：该模块如何使用，需要再考虑设计下。

3.活动邀请：该模块如何使用，需要再考虑设计下。

4.邀请记录：该模块如何使用，需要再考虑设计下。

5.活动配置
	A.查询条件不起作用。
	B.列表：
		B1.本模块字段少，能够第一层展示的都在第一层中进行展示。
		B2.删除 操作时需要增加其它模块是否已有关联，关联时给出提醒。
		B3.类型 等级，字段字典没有翻译。
	C.新增/编辑：
		C1.需增加 状态 字段，用来区分该活动是否可用可选。
		C2.新增/编辑 时 标题 需要增加重复校验。

五.审核管理：该模块下的子模块如何使用，需要再考虑设计下。

六.资讯管理
1.前沿治疗：保存时很多字段都没有保存成功，暂时不做过多功能测试。

2.广告管理
	A.查询条件不起作用。
	B.列表：
		B1.删除 操作时需要增加其它模块是否已有关联，关联时给出提醒。
	C.新增/编辑：
		C1.新增/编辑 时 标题 需要增加重复校验。
		C2.新增/编辑 时 简要说明 未保存成功。
3.新药临床中心：保存时很多字段都没有保存成功，暂时不做过多功能测试。

4.疾病科普：保存时很多字段都没有保存成功，暂时不做过多功能测试。

七.工具管理
1.医保政策管理
	A.查询条件不起作用。
	B.列表：
		B1.删除 操作时需要增加其它模块是否已有关联，关联时给出提醒。
		B2.文件 字段显示为文件名，并提供下载操作。
		B3.本模块字段少，能够第一层展示的都在第一层中进行展示。
	C.新增/编辑：
		C1.新增/编辑 时 发布时间 未保存成功。
		C2.需增加 状态 字段，用来区分该记录是否可用可选。

2.医保药物目录管理
	A.查询条件不起作用。
	B.列表：
		B1.删除 操作时需要增加其它模块是否已有关联，关联时给出提醒。
		B2.本模块字段少，能够第一层展示的都在第一层中进行展示。
	C.新增/编辑：
		C1.新增/编辑 时 药物名称 需要增加重复校验。
		C2.需增加 状态 字段，用来区分该记录是否可用可选。

3.就医指南管理
	A.查询条件不起作用。
	B.列表：
		B1.删除 操作时需要增加其它模块是否已有关联，关联时给出提醒。
		B2.本模块字段少，能够第一层展示的都在第一层中进行展示。
		B3.文件未显示 字段显示为文件名，并提供下载操作。
	C.新增/编辑：
		C1.新增/编辑 时 药物名称 需要增加重复校验。
		C2.需增加 状态 字段，用来区分该记录是否可用可选。
八.系统管理
1.用户管理
	A.列表：
		A1.第一行展示字段太少，需要增加：用户名、名称、所属单位、所属部门、手机号、状态。
		A2.展开后除第一行已显示的字段与密码外，其余字段全部展示。
	B.新增/编辑：
		B1.新增/编辑时，负责部门 所属部门 角色 没有保存成功。
		B2.新增/编辑时，需增加 名称不允许重复校验。
		B3.新增时，需增加用户名不允许重复。
		B4.新增/编辑时，单位、负责部门及所属部门需要随 单位 选择值进行联动。
		B5.编辑时，需带上原来的密码和确认密码，不用每次都需要输入一遍。
		B6.提醒方式，需要修改为多选。
		B7.新增/编辑时，需根据更新时间倒排序。
		B8.编辑时，用户名不允许修改。

2.角色管理
	A.增加查询条件框。
	B.列表：
		B1.该模块字段少，尽量第一层全部展示出来。
	C.新增/编辑：
		C1.与验证 名称 不允许重复。
	D.删除：
		D1.需增加是否被其它模块引用验证，如：用户已授权等，已引用不允许删除。
	E.角色与功能的授权操作在什么地方？

3.单位管理
	A.增加查询条件框。
	B.新增/编辑：
		B1.新增/编辑保存时报错，无法保存。
	D.删除：
		D1.需增加是否被其它模块引用验证，如：用户已授权等，已引用不允许删除。
	E.角色与功能的授权操作在什么地方？

4.部门管理
	A.增加查询条件框。
	B.新增/编辑：
		B1.新增保存时报错，名称重复校验提示不合理，不要用占位符。
		B2.编辑时，需增加 同单位下名称不能充分校验。
	C.删除：
		C1.需增加是否被其它模块引用验证，如：用户已授权等，已引用不允许删除。

九.公共问题
1.各模块查询条件框，如果只有一个框的，希望能够支持该模块全字段模糊匹配，不需要精确匹配。
2.各模块中需要考虑增加导出查询结果功能。
3.右上角除推出登录外，需增加修改密码快速入口。
4.各模块考虑增加字段排序功能。
5.各模块列表中主键ID不要显示，调试时考虑用F12就可以。

